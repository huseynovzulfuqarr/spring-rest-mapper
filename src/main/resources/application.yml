spring:
  application:
    name: spring-model-mapper



  datasource:
    url: jdbc:mysql://localhost:3306/db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
